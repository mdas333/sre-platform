apiVersion: v1
kind: ServiceAccount
metadata:
  name: platform-api
  namespace: sre-platform
  annotations:
    description: "Runtime identity for the Platform API. Bound to the Vault role 'platform-api' via the Kubernetes auth method."
automountServiceAccountToken: true
