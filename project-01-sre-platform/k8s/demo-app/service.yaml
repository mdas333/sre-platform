apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: sre-platform
  labels:
    app.kubernetes.io/name: demo-app
    app.kubernetes.io/part-of: sre-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demo-app
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
